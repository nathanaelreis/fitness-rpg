<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /

  # Não reescrever se for um arquivo real
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d

  # Reescrever todas as requisições para index.html
  RewriteRule ^ index.html [QSA,L]
</IfModule>
